{
    "builds": [
        {
            "src": "./dynamicBadges/main.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/dynamicBadges/(.*)",
            "dest": "/dynamicBadges"
        }
    ]
}
